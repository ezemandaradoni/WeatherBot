services:
  - type: worker
    name: telegram-snow-alert-bot
    runtime: node
    plan: starter
    region: oregon
    rootDir: SnowBot
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: snow-bot-data
      mountPath: /opt/render/project/src/render-data
      sizeGB: 1
    envVars:
      - key: CHECK_INTERVAL_MINUTES
        value: "15"
      - key: DATA_DIR
        value: /opt/render/project/src/render-data
      - key: WEATHER_API_KEY
        sync: false
      - key: SNOW_TELEGRAM_BOT_TOKEN
        sync: false
      - key: SNOW_TELEGRAM_CHAT_ID
        sync: false
